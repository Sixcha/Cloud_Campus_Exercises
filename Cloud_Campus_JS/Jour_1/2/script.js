const nickname = "Paul"
let score = 0

// Le nom du joueur ne changera pas, mais le score peut changer, donc l'utilisation de const pour le nom et let pour le score

console.log( "Le joueur" + nickname + "commence avec un score de " + score)
